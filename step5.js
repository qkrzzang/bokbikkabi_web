const fs = require("fs"); let content = fs.readFileSync("components/CameraButton.tsx", "utf8"); const lastFragment = content.lastIndexOf("</>"); if (lastFragment > 0) { const modalUI = "\n\n      {/* Alert Modal */}\n      {alertModal.show && (\n        <div className={styles.alertModalOverlay}>\n          <div className={styles.alertModal}>\n            <div className={styles.alertModalContent}>\n              <p className={styles.alertModalMessage}>{alertModal.message}</p>\n              <button className={styles.alertModalButton} onClick={() => setAlertModal({ show: false, message: \"\" })}>\n                OK\n              </button>\n            </div>\n          </div>\n        </div>\n      )}"; content = content.slice(0, lastFragment) + modalUI + "\n" + content.slice(lastFragment); fs.writeFileSync("components/CameraButton.tsx", content, "utf8"); console.log("Step 5: Added alertModal UI"); } else { console.log("Fragment not found"); }
